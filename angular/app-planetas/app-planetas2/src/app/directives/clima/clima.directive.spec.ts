import { ClimaDirective } from './clima.directive';

describe('ClimaDirective', () => {
  it('should create an instance', () => {
    const directive = new ClimaDirective();
    expect(directive).toBeTruthy();
  });
});
