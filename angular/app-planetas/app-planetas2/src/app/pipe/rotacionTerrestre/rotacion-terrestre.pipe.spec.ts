import { RotacionTerrestrePipe } from './rotacion-terrestre.pipe';

describe('RotacionTerrestrePipe', () => {
  it('create an instance', () => {
    const pipe = new RotacionTerrestrePipe();
    expect(pipe).toBeTruthy();
  });
});
